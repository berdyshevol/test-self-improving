# Learning Reports

One report per learning pass, written by the Learning Agent (via `/learn`).

- **Filename:** `YYYY-MM-DD-learning-slug.md`
- **Contents:** which task report was processed, the lessons extracted, and which `memory/`
  files were updated.

These document *what was learned and why*, so the Evolution Agent (`/evolve`) can spot
recurring themes over time.
